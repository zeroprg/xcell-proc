# Excel Email Sender

Автоматическая система для извлечения email адресов из Excel файлов и отправки писем с вложениями.

## Описание проекта

Проект предназначен для автоматизации процесса:
1. Чтение Excel файлов из указанной папки
2. Извлечение email адресов из файлов
3. Отправка персонализированных писем с вложениями

## Возможности

- ✅ Обработка файлов Excel (.xlsx, .xls)
- ✅ Автоматическое определение колонок с email
- ✅ Поддержка шаблонов писем
- ✅ Отправка вложений
- ✅ Логирование всех операций
- ✅ Обработка ошибок и повторные попытки отправки
- ✅ Конфигурация через файл настроек

## Требования

- Python 3.8+
- pip (менеджер пакетов Python)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd xcell-proc
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка

1. Скопируйте файл конфигурации:
```bash
cp config/config.example.json config/config.json
```

2. Отредактируйте `config/config.json` и укажите:
   - SMTP сервер и учетные данные
   - Путь к папке с Excel файлами
   - Путь к папке с вложениями
   - Настройки шаблона письма

3. Подготовьте шаблон письма в `templates/email_template.html`

## Структура проекта

```
xcell-proc/
├── src/
│   ├── __init__.py
│   ├── main.py              # Главный модуль
│   ├── excel_reader.py      # Чтение Excel файлов
│   ├── email_sender.py      # Отправка писем
│   └── utils.py             # Вспомогательные функции
├── config/
│   ├── config.json          # Конфигурация (не в git)
│   └── config.example.json  # Пример конфигурации
├── templates/
│   └── email_template.html  # Шаблон письма
├── logs/
│   └── .gitkeep
├── data/
│   └── .gitkeep             # Папка для Excel файлов
├── attachments/
│   └── .gitkeep             # Папка для вложений
├── tests/
│   └── test_main.py         # Тесты
├── .gitignore
├── requirements.txt
└── README.md
```

## Использование

### Основной режим

Запустите основной скрипт:
```bash
python src/main.py
```

### Опции командной строки

```bash
# Обработать конкретный файл
python src/main.py --file data/contacts.xlsx

# Указать другой файл конфигурации
python src/main.py --config config/custom_config.json

# Тестовый режим (без отправки писем)
python src/main.py --test

# Подробный вывод
python src/main.py --verbose
```

## Формат Excel файла

Excel файл должен содержать колонки:
- `email` или `Email` или `E-mail` - адрес электронной почты (обязательно)
- `name` или `Name` - имя получателя (опционально)
- `company` - название компании (опционально)

Пример:

| Email | Name | Company |
|-------|------|---------|
| user@example.com | Иван Иванов | ООО "Рога и Копыта" |
| admin@test.com | Петр Петров | ИП Петров |

## Шаблон письма

Шаблон письма поддерживает переменные:
- `{name}` - имя получателя
- `{email}` - email получателя
- `{company}` - название компании
- `{date}` - текущая дата

Пример:
```html
<p>Здравствуйте, {name}!</p>
<p>Ваш email: {email}</p>
```

## Логирование

Все операции записываются в файл `logs/app.log`:
- Информация о прочитанных файлах
- Список отправленных писем
- Ошибки и предупреждения

## Безопасность

⚠️ **Важно:**
- Никогда не добавляйте `config/config.json` в git
- Используйте переменные окружения для чувствительных данных
- Храните пароли в безопасном месте

## Решение проблем

### Ошибка подключения к SMTP
- Проверьте правильность настроек SMTP сервера
- Убедитесь, что разрешена отправка через SMTP
- Проверьте настройки брандмауэра

### Не находятся email в Excel
- Убедитесь, что колонка называется `email`, `Email` или `E-mail`
- Проверьте формат email адресов

### Ошибки при чтении Excel
- Убедитесь, что файл не открыт в другой программе
- Проверьте формат файла (.xlsx или .xls)

## Разработка

### Запуск тестов
```bash
pytest tests/
```

### Форматирование кода
```bash
black src/
flake8 src/
```

## Лицензия

MIT License

## Автор

Ваше имя / Ваша компания

## Поддержка

Для вопросов и предложений создавайте issues в репозитории.
