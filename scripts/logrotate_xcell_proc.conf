# Sample logrotate configuration for xcell-proc logs
# Install this content as /etc/logrotate.d/xcell-proc (requires root) or use similar on your system.
/home/username/git-rep/xcell-proc/logs/*.log {
  daily
  rotate 14
  copytruncate
  compress
  missingok
  notifempty
}
